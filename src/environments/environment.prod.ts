export const environment = {
  production: true,
  baseUrl: 'https://dominio-api.com/', // colocar aqui a api de producao
  baseApiUrl: 'https://dominio-api.com/api/' // colocar aqui a api de producao
};
